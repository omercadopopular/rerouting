# Archived tests

These 31 tests exercised superseded exploratory modules. They are retained for provenance but are not collected by the active test suite. Active release-contract tests live in `tests/test_locked_replication_pipeline.py`.
